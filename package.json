{
  "name": "cf_ai_orbit_concierge",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered Cloudflare app with Workers AI, Durable Objects memory, and premium chat UI.",
  "scripts": {
    "dev": "wrangler dev --local",
    "dev:remote": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260401.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.12.0"
  }
}
